import { MOCK_USERS } from '@shared/mocks/data'
import type { AuthUser } from './types'

// User id=1 (Aleksei Volkov) is the mock authenticated user
export const MOCK_AUTH_USER: AuthUser = MOCK_USERS[0]
